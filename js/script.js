"use strict";

let obj = {
  name: "test",
  age: 27,
  city: "Kiyv",
  colors: {
    border: "red",
    background: "grey",
  },
};

// подсчет количества ключей в обьекте 

console.log(Object.keys(obj).length)
